<Project Sdk="Microsoft.NET.Sdk.BlazorWebAssembly">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <RootNamespace>FastCom.Client</RootNamespace>
    <!-- âڑ ï¸ڈ ظ…ظ‡ظ…: ط§ظ„ظ€ Client ط¨ظٹطھظ†ط´ط± ط¬ظˆظ‡ wwwroot ط¨طھط§ط¹ ط§ظ„ظ€ Server -->
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Components.WebAssembly" Version="8.0.11" />

    <!-- ًں”‘ AuthenticationStateProvider / AuthorizeRouteView / CascadingAuthenticationState
         âڑ ï¸ڈ ط§ظ„ط­ط²ظ…ط© ط¯ظٹ ظ…ط´ transitively â€” ظ„ط§ط²ظ… طھطھط¶ط§ظپ طµط±ط§ط­ط© -->
    <PackageReference Include="Microsoft.AspNetCore.Components.Authorization" Version="8.0.11" />

    <!-- ًں”‘ IHttpClientFactory + AddHttpClient + AddHttpMessageHandler -->
    <PackageReference Include="Microsoft.Extensions.Http" Version="8.0.1" />
    <PackageReference Include="Microsoft.AspNetCore.Components.WebAssembly.DevServer" Version="8.0.11" PrivateAssets="all" />

    <!-- ًںژ¨ ط§ظ„ظ€ UI Framework -->
    <PackageReference Include="MudBlazor" Version="7.15.0" />
    <PackageReference Include="Blazor-ApexCharts" Version="4.0.1" />
    <!-- ًں“ٹ ط§ظ„ط±ط³ظˆظ… ط§ظ„ط¨ظٹط§ظ†ظٹط©: âڈ³ ظ‡طھطھط¶ط§ظپ ظپظٹ Step 12 (Dashboard)
         âڑ ï¸ڈ Blazor-ApexCharts 3.6.1 ظ…ط´ ظ…ظˆط¬ظˆط¯ ط¹ظ„ظ‰ NuGet (ط£ط­ط¯ط« ط¥طµط¯ط§ط± 4.0.0) -->
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\FastCom.Shared\FastCom.Shared.csproj" />
  </ItemGroup>

</Project>


{
  "_comment": "الـ ConnectionString الحقيقي في user-secrets (Development) — dotnet user-secrets list --project src/FastCom.Server",
  "Serilog": {
    "MinimumLevel": {
      "Default": "Debug",
      "Override": {
        "Microsoft.EntityFrameworkCore": "Information"
      }
    }
  }
}
